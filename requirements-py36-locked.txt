# Python 3.6 legacy lock (compatible set to avoid numpy/ABI errors)
numpy==1.19.5
scipy==1.5.4
joblib==1.1.0
threadpoolctl==2.2.0
scikit-learn==0.24.2
# Optional: if OpenCV via pip works on your machine (x86_64). Prefer system OpenCV on Jetson.
opencv-python==4.5.5.64
